{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bundle client",
            "type": "shell",
            "command": "bash bundle",
            "options": {
                "cwd": "${workspaceFolder}/joined/tools/js/client"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}